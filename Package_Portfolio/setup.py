from setuptools import setup

setup(name = "point-distance",
        version = "1.0",
        description = "Find distance between points",
        packages = ["point-distance"],
        author = "Abdullah Reza"
        zip_safe = False)